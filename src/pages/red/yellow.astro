---
import Room from '../../layouts/Room.astro';
---

<Room branch="red" slug="yellow">
  <div class="stage">
    <p class="holding">Мягкая, но резкая вспышка при взаимодействии.</p>
  </div>
</Room>

<style>
  .stage {
    flex: 1;
    display: grid;
    place-items: center;
    padding: 0 var(--pad-x);
  }
  .holding {
    max-width: 30ch;
    text-align: center;
    font-family: var(--font-display);
    font-size: clamp(15px, 2vw, 19px);
    line-height: 1.7;
    color: var(--ink-dim);
    opacity: 0.5;
  }
</style>
